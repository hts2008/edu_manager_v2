# Model Preferences

- Use Stitch with `modelId=GEMINI_3_1_PRO` for UX exploration.
- Use Codex local implementation for React/Vite/Tailwind code changes.
- Use subagents for read-only audits or disjoint file ownership when the user
  explicitly authorizes agent coordination.
