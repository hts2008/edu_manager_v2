# Project Insights

- The app now has broad API and UI coverage, but production readiness must be
  judged per workflow, not by route count.
- The highest leverage UX work is shared primitives plus page-level dataflow
  fixes, not another shell-only redesign.
- SMS/Zalo reminder code should remain preview/send-disabled until provider and
  opt-in policy are ready.
